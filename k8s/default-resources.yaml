apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
spec:
  limits:
  - default:
      memory: 256Mi
      cpu: 100m
    defaultRequest:
      memory: 256Mi
      cpu: 10m
    type: Container
